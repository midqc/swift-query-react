import React, { useRef } from 'react';
import EditableDiv from '../ui/EditableDiv';

const SearchBar = () => {
  return (
    <EditableDiv defaultValue="Search or -query" />
  );
}

export default SearchBar;